# 앱은 anon key 만 쓴다. service role key 는 절대 넣지 않는다(번들은 뜯어볼 수 있다).
# 값을 바꾸면 Release 번들에 인라인된 이전 값이 남는다 → expo start --clear / Xcode Clean Build.
EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
